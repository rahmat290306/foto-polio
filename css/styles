/* styles.css */

/* Reset dasar untuk konsistensi */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body dan font */
body {
    font-family: 'Inknut Antiqua', serif;
    line-height: 1.6;
    color: #333;
    background-color: #f4f4f4;
}

/* Navigasi */
nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
    background-color: #333;
    padding: 10px;
}

nav li {
    margin: 0 15px;
}

nav a {
    color: white;
    text-decoration: none;
    font-weight: 500;
}

nav a:hover {
    color: #ddd;
}

/* Header */
header {
    background-color: #fff;
    padding: 50px 0;
    text-align: center;
}

header h1 {
    font-size: 2.5em;
    margin-bottom: 20px;
}

header ul {
    list-style: none;
    padding: 0;
}

header li {
    margin: 10px 0;
    font-size: 1.2em;
}

.button {
    display: inline-block;
    background-color: #333;
    color: white;
    padding: 10px 20px;
    text-decoration: none;
    border-radius: 5px;
    margin-top: 20px;
}

.button:hover {
    background-color: #555;
}

/* Sections umum */
section {
    padding: 50px 0;
}

section h1, section h2 {
    text-align: center;
    margin-bottom: 20px;
    font-size: 2em;
}

section p {
    margin-bottom: 20px;
    text-align: center;
}

/* Gambar umum */
img {
    max-width: 100%;
    height: auto;
}

/* Section Anggota Keluarga */
.skill-list {
    list-style: none;
    text-align: center;
}

.skill-list li {
    margin: 10px 0;
    font-size: 1.2em;
}

/* Section Hobi */
#hobi figure {
    text-align: center;
    margin-top: 20px;
}

/* Section Foto Bersama Keluarga */
/* Untuk membuat gambar seragam, sejajar, dan turun ke bawah */
#keluarga .row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
}

#keluarga figure {
    flex: 1 1 calc(33.333% - 20px); /* 3 gambar sejajar, turun ke bawah jika layar kecil */
    min-width: 250px; /* Ukuran minimum untuk seragam */
    text-align: center;
}

#keluarga img {
    width: 100%; /* Ukuran seragam */
    height: auto;
    border-radius: 5px; /* Opsional untuk estetika */
}

/* Jika ada gambar di luar row, pastikan juga seragam */
#keluarga figure:not(.col-sm figure) {
    flex: 1 1 100%; /* Gambar ketiga turun ke bawah */
}

/* Section Karya Saya */
#karya ul {
    list-style: none;
    padding: 0;
    text-align: center;
}

#karya li {
    margin: 10px 0;
}

#karya figure {
    text-align: center;
    margin-top: 20px;
}

/* Section Jualan */
#jualan ul {
    list-style: none;
    padding: 0;
    text-align: center;
}

#jualan li {
    margin: 10px 0;
}

#jualan figure {
    text-align: center;
    margin-top: 20px;
}

/* Projects */
.project {
    background-color: #fff;
    margin: 20px 0;
}

.project.blue {
    background-color: #e0f7fa;
}

.project .row {
    display: flex;
    align-items: center;
}

.project .col-sm {
    flex: 1;
    padding: 20px;
}

.right-alignment {
    text-align: right;
}

/* Footer */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 20px 0;
}

footer h2 {
    font-size: 1.5em;
    margin-bottom: 10px;
}

footer p {
    font-size: 0.9em;
}

/* Responsive */
@media (max-width: 768px) {
    nav ul {
        flex-direction: column;
    }
    
    header .row {
        flex-direction: column;
    }
    
    .project .row {
        flex-direction: column;
    }
    
    #keluarga .row {
        flex-direction: column; /* Semua gambar turun ke bawah di mobile */
    }
}
